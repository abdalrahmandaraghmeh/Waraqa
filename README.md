# Waraqa
Clone the repo.

Copy src/main/resources/application.yml.example and rename it to application.yml.

Fill in their local PostgreSQL credentials.

Run docker compose up -d to start the database container.

Run the Spring Boot application.